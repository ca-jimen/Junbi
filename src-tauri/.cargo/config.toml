# Redirect build artifacts to a non-OneDrive path.
# Windows Application Control blocks execution of compiled build scripts
# in cloud-synced directories (OneDrive), so we build to a local path instead.
[build]
target-dir = "C:/cargo-target/junbi"

# On Windows with OneDrive, redirect build artifacts to avoid Windows Application Control
# blocking compiled build scripts in cloud-synced directories:
# [build]
# target-dir = "C:/cargo-target/junbi"

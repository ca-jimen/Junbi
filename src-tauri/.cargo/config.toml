# Cargo configuration for Junbi.
#
# Platform-specific settings (e.g. Windows build artifact redirection) are
# handled in scripts/tauri.js via environment variables so that a single
# config file works on all operating systems without manual overrides.
